nats_uri: nats://nats:nats@<%=node[:ipaddress]%>:4222/
logging:
  level: debug

pid_filename: /var/vcap/sys/run/stager.pid

max_staging_duration: 120
max_active_tasks: 10

queues:
  - staging

dirs:
  tmp: /var/vcap/data/stager/tmp

ruby_path: /root/cloudfoundry/.deployments/allinone/deploy/rubies/ruby-1.9.2-p180/bin/ruby

secure: false
